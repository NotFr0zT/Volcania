/*const { MessageEmbed } = require('discord.js')

const db = require('quick.db')

module.exports = {
    name: 'work',
    category: 'Economy',
    description: 'Works at your daily job!',
    aliases: [],
    usage: 'work',
    userperms: [],
    botperms: [],
    run: async (client, message, args) => {
    }
}

Math.floor(Math.random() * 500)
*/